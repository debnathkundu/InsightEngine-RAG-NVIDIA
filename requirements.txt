# Core dependencies for RAG agent
langchain>=0.1.0
langchain-community>=0.0.10
# Conversational AI support
langchain-core>=0.1.0

# PDF processing
PyPDF2>=3.0.0
pypdf>=3.17.0

# Document processing for various file types
unstructured[docx,pptx]>=0.12.0
python-magic>=0.4.27
langchain-unstructured>=0.0.1
# For image support (optional, uncomment to enable)
# pytesseract>=0.3.10
# pillow>=10.0.0

# Vector database
faiss-cpu>=1.7.0

# Hybrid search (BM25 + Vector search)
rank-bm25>=0.2.2

# Document re-ranking with cross-encoders
sentence-transformers>=2.2.0

# Environment management
python-dotenv>=1.0.0

# HTTP requests for NVIDIA API
requests>=2.31.0

# Additional utilities
numpy>=1.21.0
tqdm>=4.65.0

# Web interface
streamlit>=1.28.0
plotly>=5.15.0

# File system monitoring
watchdog>=2.3.0

# Note on libmagic:
# The `python-magic` library requires the `libmagic` system library to be installed.
# On macOS, this can be installed with Homebrew: `brew install libmagic`
# On Debian/Ubuntu, use: `sudo apt-get install libmagic1`
# This is a system dependency and cannot be installed via pip.
